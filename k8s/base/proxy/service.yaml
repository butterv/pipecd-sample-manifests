apiVersion: v1
kind: Service
metadata:
  name: gitops-sample-gateway-active
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: gitops-sample-gateway
---
apiVersion: v1
kind: Service
metadata:
  name: gitops-sample-gateway-preview
spec:
  type: NodePort
  ports:
    - port: 8081
      targetPort: 8080
  selector:
    app: gitops-sample-gateway
